% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_default_input.R
\name{get_default_input}
\alias{get_default_input}
\title{Get the Default Input for a Model}
\usage{
get_default_input(model_path = NULL, access_key = NULL, server_url = NULL)
}
\arguments{
\item{model_path}{Character. Model identifier in \code{"namespace/model"} format.
Falls back to the value stored by \code{\link[=connect_to_model]{connect_to_model()}}.}

\item{access_key}{Character. API bearer token. Falls back to the stored key
or the \code{MODELSCLOUD_ACCESS_KEY} environment variable.}

\item{server_url}{Character. Server base URL. Falls back to the stored URL.}
}
\value{
The default input object as returned by the model, with its original
R class preserved.
}
\description{
Retrieves the default input values that the model uses when no custom input
is supplied to \code{\link[=model_run]{model_run()}}. Useful for discovering what parameters a model
accepts and inspecting their baseline values before customising a run.

All arguments fall back to the values stored by \code{\link[=connect_to_model]{connect_to_model()}} when
not explicitly supplied.
}
\examples{
\dontrun{
connect_to_model("examples/toymodel2", access_key = "YOUR_KEY")
defaults <- get_default_input()
str(defaults)
}
}
\seealso{
\code{\link[=connect_to_model]{connect_to_model()}}, \code{\link[=get_sample_input]{get_sample_input()}}, \code{\link[=model_run]{model_run()}}
}
