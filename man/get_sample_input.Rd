% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sample_input.R
\name{get_sample_input}
\alias{get_sample_input}
\title{Get a Sample Input for a Model}
\usage{
get_sample_input(model_path = NULL, access_key = NULL, server_url = NULL, ...)
}
\arguments{
\item{model_path}{Character. Model identifier in \code{"namespace/model"} format.
Falls back to the value stored by \code{\link[=connect_to_model]{connect_to_model()}}.}

\item{access_key}{Character. API bearer token. Falls back to the stored key
or the \code{MODELSCLOUD_ACCESS_KEY} environment variable.}

\item{server_url}{Character. Server base URL. Falls back to the stored URL.}

\item{...}{Additional arguments passed to the model's \code{get_sample_input}
function (e.g. \code{n = 5} to limit the number of rows returned).}
}
\value{
The sample input object as returned by the model, with its original
R class preserved (typically a data frame).
}
\description{
Retrieves a sample (example) set of inputs from the model on the server.
The result is deserialised from RDS format, so the original R object class
(e.g. data frame) is fully preserved.

All arguments fall back to the values stored by \code{\link[=connect_to_model]{connect_to_model()}} when
not explicitly supplied.
}
\examples{
\dontrun{
connect_to_model("mohsenss/qrisk3pexa", access_key = "YOUR_KEY")
sample <- get_sample_input()
result <- model_run(model_input = sample)
}
}
\seealso{
\code{\link[=connect_to_model]{connect_to_model()}}, \code{\link[=get_default_input]{get_default_input()}}, \code{\link[=model_run]{model_run()}}
}
