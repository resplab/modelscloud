% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_async_results.R
\name{get_async_results}
\alias{get_async_results}
\title{Retrieve the Result of an Asynchronous Model Run}
\usage{
get_async_results(
  job,
  wait = FALSE,
  interval = 2,
  timeout = 300,
  access_key = NULL,
  server_url = NULL
)
}
\arguments{
\item{job}{The job handle returned by \code{model_run(async = TRUE)} (an object
of class \code{pexa_result_async}).}

\item{wait}{Logical. If \code{FALSE} (default), check once and return either the
decoded result or the unchanged job handle. If \code{TRUE}, poll until the job
finishes or \code{timeout} is reached.}

\item{interval}{Numeric. Seconds between polls when \code{wait = TRUE}.
Default \code{2}.}

\item{timeout}{Numeric. Maximum seconds to wait when \code{wait = TRUE}. Default
\code{300}. Set to \code{Inf} to wait indefinitely. On timeout the job handle is
returned (with a warning) so you can keep polling.}

\item{access_key}{Character. API bearer token. Falls back to the stored key
or the \code{MODELSCLOUD_ACCESS_KEY} environment variable.}

\item{server_url}{Character. Server base URL. Falls back to the stored URL.}
}
\value{
If the job is finished: the model output with its original R class
preserved (the raw server response is attached as an invisible attribute
for \code{\link[=get_plots]{get_plots()}}). If still running: the job handle, unchanged.
}
\description{
Fetches the result of a job submitted with \code{model_run(async = TRUE)}. Pass
the job handle returned by \code{\link[=model_run]{model_run()}} directly.

By default this checks once: if the job is finished it returns the decoded
result (with its original R class preserved, exactly like a synchronous
\code{\link[=model_run]{model_run()}}); if the job is still running it returns the job handle
unchanged, so you can call \code{get_async_results()} again later to poll.

Set \code{wait = TRUE} to block and poll automatically until the job completes
(or \code{timeout} is reached).
}
\examples{
\dontrun{
connect_to_model("mohsenss/epicrpexa", access_key = "YOUR_KEY")

# Submit asynchronously per call, then poll manually
job <- model_run(async = TRUE)
result <- get_async_results(job)
if (inherits(result, "pexa_result_async")) {
  Sys.sleep(5)
  result <- get_async_results(job)
}

# Or block until done
result <- get_async_results(model_run(async = TRUE), wait = TRUE)
}
}
\seealso{
\code{\link[=model_run]{model_run()}}, \code{\link[=connect_to_model]{connect_to_model()}}, \code{\link[=get_plots]{get_plots()}}
}
