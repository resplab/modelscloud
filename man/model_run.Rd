% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_run.R
\name{model_run}
\alias{model_run}
\title{Run a ModelsCloud Model}
\usage{
model_run(
  model_path = NULL,
  model_input = NULL,
  func_name = NULL,
  access_key = NULL,
  server_url = NULL,
  async = NULL
)
}
\arguments{
\item{model_path}{Character. Model identifier in \code{"namespace/model"} format.
Falls back to the value stored by \code{\link[=connect_to_model]{connect_to_model()}}.}

\item{model_input}{Named list of input parameters to pass to the model. If
\code{NULL} the model uses its own built-in defaults.}

\item{func_name}{Character. The function within the model to invoke. If
\code{NULL} the model's default function is called.}

\item{access_key}{Character. API bearer token. Falls back to the stored key
or the \code{MODELSCLOUD_ACCESS_KEY} environment variable.}

\item{server_url}{Character. Server base URL. Falls back to the stored URL.}

\item{async}{Logical. Run asynchronously? Falls back to the value from
\code{\link[=connect_to_model]{connect_to_model()}} (default \code{FALSE}).}
}
\value{
For synchronous calls a \code{pexa_result} object. For asynchronous
calls a \code{pexa_result_async} object — call \code{\link[pexaclient:get_async_results]{get_async_results()}} to poll
until the job completes.
}
\description{
Executes a model on the ModelsCloud platform and returns its result. All
arguments fall back to the values stored by \code{\link[=connect_to_model]{connect_to_model()}} when not
explicitly supplied, so in a typical session you only need to call
\code{model_run()} with no arguments (or just \code{model_input}).

For asynchronous jobs the function returns immediately with a
\code{pexa_result_async} object. Use \code{\link[pexaclient:get_async_results]{get_async_results()}} to poll for
completion.
}
\examples{
\dontrun{
connect_to_model("resp/epicr", access_key = "YOUR_KEY")

# Run with model defaults
result <- model_run()

# Run with custom inputs
result <- model_run(model_input = list(time_horizon = 15, discount_rate = 0.03))

# Override async for a single call
job <- model_run(async = TRUE)
result <- get_async_results(job)
}
}
\seealso{
\code{\link[=connect_to_model]{connect_to_model()}}, \code{\link[pexaclient:get_async_results]{get_async_results()}},
\code{\link[pexaclient:list_extra_output]{list_extra_output()}}, \code{\link[pexaclient:get_extra_output]{get_extra_output()}}
}
